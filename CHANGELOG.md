# Changelog

本文件记录用户可感知的变化。正式发布版本号确定后，再将 `Unreleased` 归档为版本条目。

## [Unreleased]

### Added

- 统一使用根命令 `./trip` 管理配置、Web/TUI/CLI、酒店、小红书、验证和管理员操作。
- 新建会话时选择快捷模式或完整攻略；顶层模式在会话内保持，两种模式都会由服务端自动生成标题。
- Web 支持本机、随机 Quick Tunnel、固定域名 Tunnel 和开发四种启动方式。
- GLM 与通用 OpenAI-compatible Provider、GLM 思考强度和管理员模型设置可配置。
- 12306 车次查询、地图与酒店图片展示、账号认证、管理员设置和选装集成状态管理已纳入当前 Web 流程。

### Changed

- README 改为当前用户手册，集中说明三步开始、四种 Web 模式、配置/安全边界、选装集成、工具和已知限制。
- `./trip setup` 改为安全、可重复的交互式配置/构建向导；`./trip setup check` 改为严格只读体检，密钥输入不回显，失败返回可信退出码。
- `config.example.toml` 的小红书默认关闭；本机 `.env`、`config.toml`、会话、地图、酒店、数据库和构建产物均不进入仓库。
- Web 前端、生产构建产物和可重建依赖由本机或部署环境生成，不作为源码提交。

### Removed

- 删除重复/废弃的启动入口和旧的脚本调用方式，统一迁移到 `./trip`。
- 不再施工携程远程二维码或短信网页登录；保留服务端人工扫码登录和安全边界说明。

### Security

- API key、Web 口令、Tunnel 信息、账号 cookies 和会话数据继续只保存在服务端或本机被忽略文件中。
- 启用账号认证后，管理员设置受权限保护，密钥接口只返回配置状态；正式公网部署应使用 HTTPS、认证和非空访问口令。
