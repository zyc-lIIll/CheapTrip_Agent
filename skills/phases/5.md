# 阶段5：完整攻略

校验车次/航班/景点预约要求（时效信息可用 `search_xhs` 查最近游客的预约/排队反馈，环境未启用则 search_web），输出**一份完整的攻略**。

**重新生成全套地图**（按最终确定的行程）：
- `generate_overview_map`：全程总览（所有 part + 大交通连线）
- 每个 part 各调 `generate_city_map`：按最终游览顺序连路线

输出完整攻略：路线地图 + 总行程表（跨 part）+ 各 part 每日行程 + 美食清单 + 预算表 + 出发前 Checklist（预约/购票/带物提醒）。

主动询问修改意见。地图技能详见 `skills/map.md`。

**本阶段完成**：方案交付给用户，本轮规划结束；用户提修改意见则回对应阶段调整。
